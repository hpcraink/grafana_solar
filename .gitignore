# Ignore the metadata file of the last import into SQL
.lastimport
# Ignore the raw data, the preprocessed data, and the processed (SQL) input.
data/
preprocessed/
preprocess/*.png
preprocess/mail_daily_settings.py
preprocess/mail_daily_settings_de.py
processed/
